═══════════════════════════════════════════════════════════════════════════
  WBLOCK SCRIPT FORMAT - BASE POINT ISSUE FIXED
  MacroManager v5.10 - November 7, 2025
═══════════════════════════════════════════════════════════════════════════

🔴 PROBLEM IDENTIFIED:

When you ran the script, AutoCAD asked for:
  1. Base point
  2. Objects to select

This happened because the script format was INCORRECT.

═══════════════════════════════════════════════════════════════════════════
WHY THIS HAPPENED
═══════════════════════════════════════════════════════════════════════════

WBLOCK has TWO modes:

MODE 1: Export Block Definition (what we want)
  ────────────────────────────────────────────────────────────────────
  -WBLOCK
  "C:\path\block.dwg"
  =BlockName            ← Equals sign means "export existing block"
  
  Result: Exports the block definition directly, NO prompts
  ────────────────────────────────────────────────────────────────────

MODE 2: Create New Block from Objects (what was happening)
  ────────────────────────────────────────────────────────────────────
  -WBLOCK
  "C:\path\block.dwg"
  (ENTER - blank line)  ← This triggered "Objects" mode
  
  AutoCAD asks:
    Specify insertion base point: (waiting for point)
    Select objects: (waiting for selection)
  
  Result: Tries to create NEW block from selected objects
  ────────────────────────────────────────────────────────────────────

═══════════════════════════════════════════════════════════════════════════
THE BUG IN SCRIPT FORMAT
═══════════════════════════════════════════════════════════════════════════

OLD SCRIPT (WRONG - had extra blank line):
  ──────────────────────────────────────────────────────────────────────
  -WBLOCK
  "C:\Asset-Eyes_Durgaram\Working_0825\2D_Electrical Automation\ICE RINK\Block Library\ICE_Asset-Eyes_Durgaram\Working_0825\2D_Electrical Automation\ICE RINK\Test\18-24\TR_480_&_600V_Rev.07.dwg"
  
  =ICE_Asset-Eyes_Durgaram\Working_0825\2D_Electrical Automation\ICE RINK\Test\18-24\TR_480_&_600V_Rev.07
  
  ──────────────────────────────────────────────────────────────────────

The blank line between the file path and =BlockName caused WBLOCK
to think you wanted "Objects" mode instead of "Block" mode.

NEW SCRIPT (CORRECT - no blank line between path and =BlockName):
  ──────────────────────────────────────────────────────────────────────
  -WBLOCK
  "C:\Asset-Eyes_Durgaram\Working_0825\2D_Electrical Automation\ICE RINK\Block Library\ICE_Asset-Eyes_Durgaram\Working_0825\2D_Electrical Automation\ICE RINK\Test\18-24\TR_480_&_600V_Rev.07.dwg"
  =ICE_Asset-Eyes_Durgaram\Working_0825\2D_Electrical Automation\ICE RINK\Test\18-24\TR_480_&_600V_Rev.07
  
  ──────────────────────────────────────────────────────────────────────

Now the =BlockName immediately follows the file path, so WBLOCK
knows to export the existing block definition.

═══════════════════════════════════════════════════════════════════════════
WHAT WAS FIXED
═══════════════════════════════════════════════════════════════════════════

In MacroManager_v5.10.lsp, line ~760:

BEFORE (WRONG):
  ────────────────────────────────────────────────────────────────────
  (write-line (strcat "-WBLOCK") script_handle)
  (write-line (strcat "\"" dwg_path "\"") script_handle)
  (write-line "" script_handle)  ;; ← EXTRA BLANK LINE (BAD!)
  (write-line (strcat "=" block_name) script_handle)
  (write-line "" script_handle)
  ────────────────────────────────────────────────────────────────────

AFTER (CORRECT):
  ────────────────────────────────────────────────────────────────────
  (write-line (strcat "-WBLOCK") script_handle)
  (write-line (strcat "\"" dwg_path "\"") script_handle)
  (write-line (strcat "=" block_name) script_handle)  ← Directly after path
  (write-line "" script_handle)  ;; Final blank to complete command
  ────────────────────────────────────────────────────────────────────

═══════════════════════════════════════════════════════════════════════════
WHAT TO DO NOW
═══════════════════════════════════════════════════════════════════════════

1. RELOAD MacroManager_v5.10.lsp:
   ────────────────────────────────────────────────────────────────────
   Type: APPLOAD
   Browse: MacroManager_v5.10.lsp
   Click: Load
   ────────────────────────────────────────────────────────────────────

2. RUN EXPORT AGAIN:
   ────────────────────────────────────────────────────────────────────
   Type: MACROMANAGER
   Select blocks
   Choose Script method
   START EXPORT
   ────────────────────────────────────────────────────────────────────

3. CHECK THE SCRIPT FILE:
   ────────────────────────────────────────────────────────────────────
   Open: export_blocks.scr in Notepad
   
   Should look like:
   -WBLOCK
   "C:\path\block.dwg"
   =BlockName
   
   -WBLOCK
   "C:\path\block2.dwg"
   =BlockName2
   
   (No blank lines between path and =BlockName!)
   ────────────────────────────────────────────────────────────────────

4. RUN THE SCRIPT:
   ────────────────────────────────────────────────────────────────────
   Type: SCRIPT
   Select: export_blocks.scr
   Click: OK
   
   NOW IT SHOULD:
   ✓ NOT ask for base point
   ✓ NOT ask for object selection
   ✓ Automatically export each block
   ✓ Create DWG files immediately
   ────────────────────────────────────────────────────────────────────

═══════════════════════════════════════════════════════════════════════════
EXPECTED BEHAVIOR (FIXED)
═══════════════════════════════════════════════════════════════════════════

When you run the script, you should see in command line:

Command: -WBLOCK
Enter name of output file: "C:\path\block.dwg"
Source object <Objects>: =BlockName

Command: -WBLOCK
Enter name of output file: "C:\path\block2.dwg"
Source object <Objects>: =BlockName2

...and so on, with NO prompts for base point or object selection.

The script will execute automatically and create all DWG files.

═══════════════════════════════════════════════════════════════════════════
CONFIRMATION
═══════════════════════════════════════════════════════════════════════════

YES, the script WAS asking for base point due to the incorrect format.

The extra blank line after the file path caused WBLOCK to enter
"Objects" mode instead of "Block" mode.

This has now been FIXED in MacroManager_v5.10.lsp.

The script will now export block definitions directly without any
prompts for base points or object selection.

═══════════════════════════════════════════════════════════════════════════
TECHNICAL DETAILS
═══════════════════════════════════════════════════════════════════════════

WBLOCK Command Syntax:
  ──────────────────────────────────────────────────────────────────────
  -WBLOCK
  "filename"
  [option]
  
  Options:
  * (asterisk)    = Export entire drawing
  = (equals)      = Export existing block definition
  (blank/ENTER)   = Create new block from objects (asks for base point)
  ──────────────────────────────────────────────────────────────────────

We use "=" option to export existing block definitions.

The blank line must come AFTER the =BlockName, not before it.

═══════════════════════════════════════════════════════════════════════════
SUMMARY
═══════════════════════════════════════════════════════════════════════════

QUESTION: "Can you confirm the script really asks for Base point?"

ANSWER: YES, it was asking for base point because of incorrect format.

CAUSE: Extra blank line between file path and =BlockName

FIX APPLIED: Removed the extra blank line

RESULT: Script will now export blocks without prompting

ACTION: Reload MacroManager_v5.10.lsp and re-export

═══════════════════════════════════════════════════════════════════════════
